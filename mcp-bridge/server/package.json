{
  "name": "cuis-mcp-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "MCP bridge process pairing Claude Code (via MCP over stdio) with a running Cuis image (via TCP/NDJSON).",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5",
    "@types/node": "^22.10.7"
  }
}
